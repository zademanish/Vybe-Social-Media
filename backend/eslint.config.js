export default [
  {
    files: ['**/*.js'], //only chech .js files
    rules: {
      // semi :"error", //force semicolons
      'no-unused-vars': 'warn', // warn if varialbles are unused
    },
  },
];
